version: '3.9'

services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=09u51a0519
      - POSTGRES_USER=admin
      - POSTGRES_DB=patlocal